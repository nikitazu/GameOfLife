<Window x:Class="Life.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Title="Game of Life"
        Height="900"
        Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.RowSpan="3">
            <Button Click="StepButtonClick">Step</Button>
            <Button Click="AutoButtonClick">Auto</Button>
            <Button Click="ResetButtonClick">Reset</Button>
        </StackPanel>
        
        <Canvas Grid.Column="1" Grid.RowSpan="3" Name="Screen" Height="800" Width="800" />
    </Grid>
</Window>
